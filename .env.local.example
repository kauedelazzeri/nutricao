# Variáveis de Ambiente
# Copie este arquivo para .env.local e preencha com seus valores reais

# Supabase Configuration
# Obtido em: https://supabase.com/dashboard/project/[YOUR-PROJECT]/settings/api
VITE_SUPABASE_URL=https://[YOUR-PROJECT-REF].supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_your-key-here

# ⚠️ NUNCA adicionar no .env.local (apenas para backend/edge functions):
# VITE_SUPABASE_SECRET_KEY=sb_secret_...
# VITE_SUPABASE_PROJECTID=...

# Cloudinary Configuration
# Obtido em: https://console.cloudinary.com/
VITE_CLOUDINARY_CLOUD_NAME=your-cloud-name
# ⚠️ IMPORTANTE: Cloud Name é só o nome (ex: dmxxxxxxxillsw), NÃO o URL completo
VITE_CLOUDINARY_UPLOAD_PRESET=nutricao_meals

# ⚠️ API Key/Secret NÃO são necessárias para unsigned upload:
# VITE_CLOUDINARY_API_KEY=... (NÃO USAR - unsigned upload não precisa)
# VITE_CLOUDINARY_API_SECRET=... (NUNCA USAR NO FRONTEND)

# Notas:
# - Nunca commitar o arquivo .env.local (já está no .gitignore)
# - Para Vercel, configurar as mesmas variáveis no dashboard (ver docs/setup/vercel-integration.md)
# - VITE_ prefix é obrigatório para variáveis expostas ao client-side
# - Use apenas PUBLISHABLE_KEY (não Secret Key) no frontend por segurança
